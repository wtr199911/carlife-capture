<%= link_to transition_path(notification) do %>
  <div class="text-center m-3" >
    <% if notification.subject.follower.guest_customer? %>
      ゲストユーザー
    <% else %>
      <%= link_to notification.subject.follower.name, customer_path(notification.subject.follower), class: "text-success" %>
    <% end %>
    があなたをフォローしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>