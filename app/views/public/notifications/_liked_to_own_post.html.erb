<%= link_to transition_path(notification) do %>
  <div class="text-center m-3">
    <%= link_to notification.subject.customer.name, customer_path(notification.subject.customer), class: "text-success" %>
    があなたの
    <%= link_to '投稿', post_path(notification.subject.post), class: "text-success" %>
    にいいねしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>