<h1>投稿フォーム</h1>
<%= form_with model: @post do |f| %>

 <h4>画像</h4>
 <%= f.file_field :image, accept: "image/*" %>

 <h4>タイトル</h4>
 <%= f.text_field :title, placeholder: "" %>

 <h4>投稿文</h4>
 <%= f.text_area :detail %>

 <h4>撮影場所</h4>
 <%= f.collection_select(:prefecture_id, Prefecture.all, :id, :name,{include_blank: "---"} ) %>
 <%= f.text_field :place, placeholder: "撮影場所を記入して下さい" %>

 <%= f.submit "投稿" %>
<% end %>